import React from 'react';

const Error = () => {
    return (
        <div>
            <h1 style={{ color: 'red' }}>
                You've gone to a page that doesn't exist!
            </h1>
        </div>
    );
};

export default Error;